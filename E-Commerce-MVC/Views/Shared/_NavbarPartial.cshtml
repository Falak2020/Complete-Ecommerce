@using E_Commerce_MVC.Models

  
<nav class="navbar navbar-expand-lg navbar-light bg-light shadow ">
    <div class="container">
       
            <a class="navbar-brand" href="#">e-Furniture</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
                <ul class="navbar-nav  ms-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Products" asp-action="Index">Products</a>
                    </li>

                    <li class="nav-item dropdown ">
                        <a class=" btn btn-outline-info me-3" asp-controller="Products" asp-action="ViewCart" asp-route-orderId=1>
                            <i class="fas fa-cart-plus"></i>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarScrollingDropdown">


                            @if (@ViewData["MyCart"] != null)
                            {
                                @foreach (var item in (List<Orderitem>)ViewData["MyCart"])

                                {
                                    <li>
                                        <a href="#"> @item.ProductId </a>
                                    </li>
                                }
                            }
                        </ul>


                    </li>
                    <li>
                        <a class="btn btn-outline-success me-3" asp-controller="Autentication" asp-action="SignIn">Log In</a>
                    </li>
                    <li>
                        <a class="btn btn-outline-primary">Register</a>
                    </li>

                </ul>

            </div>
            

            @* <a data-toggle="dropdown" class="dropdown-toggle me-3">
                 <i class="fas fa-cart-plus"></i>
                <ul class="dropdown-menu">
                     @if (@ViewData["Cart"] != null)
                     {
                         @foreach (var item in (List<ShoppingCartItem>)ViewData["Cart"])

                         {
                             <li> <a href="#"> @item.product.Name</a></li>
                         }
                     }
                 </ul>


                    </a>
                    </li>

                    </ul>*@



        </div>

</nav>

<script>

</script> 